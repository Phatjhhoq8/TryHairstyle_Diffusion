# ==============================================
# TryHairStyle Backend - Python Dependencies
# ==============================================
# Dùng cho cả LOCAL và DOCKER:
#   - Local: pip install -r backend/requirements.txt
#   - Docker: Dockerfile cài torch riêng từ CUDA index,
#             rồi pip install -r (sẽ skip torch đã có)
# ==============================================

# Core Backend Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.6.0
python-dotenv>=1.0.1

# Task Queue & Async
celery>=5.3.6
redis>=5.0.1

# PyTorch Ecosystem (CUDA 12.1)
# Local: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Docker: Dockerfile đã cài sẵn, pip sẽ skip
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# Generative AI & Diffusers
diffusers>=0.36.0
transformers>=5.0.0
accelerate>=1.0.0
peft>=0.18.0
safetensors>=0.7.0
huggingface_hub>=1.0.0

# Computer Vision
opencv-python-headless>=4.9.0
pillow>=10.2.0
numpy>=2.0.0
scikit-image>=0.24.0,<=0.25.2
insightface>=0.7.3
ultralytics>=8.0.0

# Face Recognition & Embedding
facenet-pytorch>=2.6.0

# ONNX Runtime
onnx>=1.16.0
onnxruntime>=1.18.0

# Perceptual Loss
lpips>=0.1.4

# VRAM Optimization (cho GPU 12GB)
xformers>=0.0.28
bitsandbytes>=0.43.0

# Translation
deep-translator>=1.11.0

# Utilities
tqdm>=4.66.0
scipy>=1.12.0
matplotlib>=3.10.0
regex>=2024.0.0
filelock>=3.13.0
typing-extensions>=4.10.0
PyYAML>=6.0.0
Cython>=3.0.0
easydict>=1.0
gdown>=5.0.0
